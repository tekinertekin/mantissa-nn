__pycache__/
*.pyc
.venv/
.venv*/
venv/
# downloaded datasets — never push; the code prints the download command
data/
*.npz
# benchmark raw results — regenerable; only assets/*.png plots are committed
bench/results/
results/
# packaging / test artifacts
build/
dist/
*.egg-info/
.pytest_cache/
# local tooling state
.claude/
